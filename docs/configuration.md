# EchoLens 配置规范

## 配置原则

所有运行参数通过配置管理，不直接写入代码。

配置来源优先级：

```text
环境变量
    ↓
配置文件
    ↓
默认值
```

## 配置分类

### AI 模型配置

包括：

- LLM Provider
- Model Name
- API Key

### 语音识别配置

包括：

- Whisper 模型
- 推理设备
- 语言设置

### 存储配置

包括：

- 数据库连接
- 文件路径
- 向量数据库

## 原则

- 敏感信息使用环境变量
- 不提交真实密钥
- 不在代码中硬编码配置
